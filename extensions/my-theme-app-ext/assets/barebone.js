// Note that imported script files by each block are global in theme storefront in browsers, which means 
// any object are global scopes that cannot conflicts with others

console.log("'assets/barebone.js'");