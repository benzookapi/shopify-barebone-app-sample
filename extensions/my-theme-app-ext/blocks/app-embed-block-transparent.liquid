{{ block.settings.barebone_app_tag }}

{% schema %}
{
  "name": "t:barebone.app_embed_transparent.name",
  "target": "head",
  "settings": [
    {
      "type": "textarea",
      "id": "barebone_app_tag",
      "label": "t:barebone.app_embed_transparent.tag",
      "default": "<script>\n// This transparent app embed block inserts tags into the document head.\n// Add code such as a Google Analytics tag here.\n</script>"
    }
  ]
}
{% endschema %}
