query Input {
  cart {
    deliveryGroups {
      selectedDeliveryOption {
        title
        handle
      }
    }
  }
  paymentMethods {
    id
    name
  }
  paymentCustomization {
    metafield(namespace: "barebone_app_function_payment", key: "filter") {
      value
    }
  }
}
