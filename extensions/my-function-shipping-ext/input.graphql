query Input {
  cart {
    deliveryGroups {
      deliveryAddress {
        zip
      }
      deliveryOptions {
        handle
        title
      }
    }
  }
  deliveryCustomization {
    metafield(namespace: "barebone_app_function_shipping", key: "filter") {
      value
    }
  }
}