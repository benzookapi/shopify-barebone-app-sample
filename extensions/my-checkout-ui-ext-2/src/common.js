// This can be used by `Checkout.jsx` and `Checkout.js` both shared with multiple files.
export const commonFuncExternal = (text) => {
    console.log(`commonFuncExternal outputs with text: ${text}`);
};